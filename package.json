{
  "name": "noderl",
  "version": "0.1.0",
  "description": "Agentic-RL substrate: trace -> reward -> memory -> repair.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --test \"test/*.test.ts\" \"packages/*/test/*.test.ts\"",
    "typecheck": "tsc -p tsconfig.packages.json",
    "demo": "node experiments/fr-a1-runs/fr-a1-live-run-001-build.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.7.2"
  }
}
